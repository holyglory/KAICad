# Recursive structural editor — design QA

final result: blocked

## Active comparison: requirement field history

The approved target is `/home/holyglory/kicad/automation/design/history-and-conflicts/02-field-history.png` (1536 × 1024 full scene, with the Routing requirements history dialog). The approved recursive System/PSU/CPU diagrams supersede the earlier option-1 layout retained below. This is native desktop UI; CSS viewport and browser density do not apply.

The first component captures are the hash-verified `native-dialog` artifacts from `t20260919T225954Z-d035cc`, materialized at `/mnt/build-storage/codex/kicad/evidence/field-history-d035cc/`. The source, `light/02-earlier-text.png`, and `dark/02-earlier-text.png` were opened together in one comparison input. Both implementation images are 426 × 330. The fixture has no resolvable source instruction, so no source-opening control is enabled. This is a component test, not the complete editor journey or its background canvas.

- P1, observation correctness: `01-current.png` and `02-earlier-text.png` have identical hashes and show v3 selected. Native control-state assertions reached v2, but capture ran before the next GTK paint. These images do not prove the intended historical state. Wait for a completed native frame and reject unchanged captures after this visible change.
- P2, sizing and typography: the dialog opens at 426 × 330 instead of its intended 740 × 520 client area. KiCad's final sizer setup fits the dialog after its preferred size is assigned. Apply preferred sizing afterward, keep a usable compact minimum, and match the reference's readable text hierarchy using the native font family.
- P1, integration: the dialog component is not yet connected to the recursive editor's History actions. Save/Decline, conflict recovery, navigation and source-document opening still require the full rendered editor journey. Passing component checks cannot close that gap.

Fidelity review: typography and layout are blocked by the incorrect initial size; colors follow native light/dark themes but need review after a valid capture; there are no decorative bitmap assets to generate; copy uses the approved History/Close/Use-text-in-draft actions. Focused comparison of the revision list, comparison text and footer will follow a matching-state capture. No visual pass is claimed from the stale frames.

Interaction evidence: the compiled model supplied revision-bound data to a real native dialog under a virtual display. Keyboard selection, Escape, restore, reopen, compact controls and cross-scope content isolation passed in both themes. The missing rendering checkpoint limits what those screenshots establish. The sealed run remains unchanged.

Next comparison: inspect the repaired 740 × 520 and compact captures, confirm the selected row and text are v2 while saved text remains v3, compare both themes, then connect the component to the editor and verify the complete journey.

### Second comparison: corrected native window captures

Run `t20260919T232148Z-1ddc15` passes the focused managed checks, native compilation and both themed GUI journeys. Hash-verified captures are at `/mnt/build-storage/codex/kicad/evidence/field-history-1ddc15/`. Source, light/dark `02-earlier-text.png` and light `03-compact.png` were opened in the same comparison input. The normal captures are 740 × 520; compact is 590 × 440. These are native window pixels at the test display's normal density, with no browser/CSS scaling. The source is a full 1536 × 1024 scene; compare its modal region, not the unimplemented background editor.

The capture now reads the actual native window after a GTK frame checkpoint. Current and earlier-state images have distinct hashes and show the correct row, heading and text. The original screen-DC captures remain sealed as failed visual evidence. Preferred sizing is applied after KiCad's sizer setup, and the native font hierarchy is larger. The observation mismatch and undersized initial dialog are repaired.

Typography is readable in both themes; the two comparison fields and footer remain visible at compact size. Colors follow the native theme with visible focus and selection. No bitmap assets are approximated; all content is native controls. Copy distinguishes selected text from saved text and restoration from saving. The fixture's absent source provider explains the absent source action; its real editor integration is still unverified. The revision list is denser and the primary button less visually emphasized than the mock; refine those with the editor integration rather than claiming full visual parity now.

The focused regions reviewed are the revision list, selected/saved text and footer actions. Component success does not qualify the editor route, source opening, Save/Decline, conflicts or a full system-level diagram journey. The overall result remains blocked on those concrete integration and visual gaps.

## Recursive editor integration comparisons

The first complete editor journey, `t20260920T000307Z-8fc24d`, exercised two native KiCad instances through MCP and real keyboard/mouse interaction. It navigated System → PSU → System → CPU, edited requirements, saved and declined drafts, restored field history, rejected a stale save while retaining the draft, and reopened the file. Its editor artifacts are materialized at `/mnt/build-storage/codex/kicad/evidence/recursive-editor-8fc24d/editor/`.

Source walkthrough and the root/PSU/CPU captures were opened in the same comparison input. P1: the right inspector clipped History buttons. P2: native body text was too small. P1: boundary interface labels overlapped connection titles, and some boundary links exited through the far side of a child. The behavior pass did not establish visual readiness.

The repaired journey `t20260920T000928Z-c56124` passes the same interactions. Its source hash is `40565337d3654f6ff67b1bf6beb1a885c23589e3c1f6bf4f3bf78533b513f784`; hash-verified captures are in `/mnt/build-storage/codex/kicad/evidence/recursive-editor-c56124/editor/`. The approved System image and `be01ee80-a268-46a9-a908-4515faae0656-recursive-system.png` were opened together with the CPU capture. Both root views select PSU. Fixture text/revision numbers differ intentionally and are not product requirements.

The implementation is a 1536 × 1024 native editor within a 1600 × 1150 virtual display; the screenshot includes the surrounding black display area. The reference is a 1536 × 1024 complete scene. No CSS/device scaling is involved. Compare the native editor region, not the extra desktop pixels.

The inspector now keeps its three History controls visible, native text is readable, and the reviewed CPU boundary labels no longer overlap. Port-side choices route toward the peer without inventing electrical direction. Main region organization follows the approved canvas/right-inspector design. Existing native icons are reused; diagram blocks and links are data-driven drawing, not raster substitutes. Save/Decline remain editing actions and invoke no agent.

Remaining P1/P2 scope at that snapshot: diagram implementation/history selection, connection selection/editing, comments/markup, saved layout manipulation, whole-diagram restore/preview, and the designed three-way conflict workflow were unfinished. That snapshot rejected stale files without resolving them. Later conflict evidence follows below. Full-window compact/dark interaction and complete visible-control coverage are not yet qualified. Field-history pagination beyond 200 changes is tracked as `p8ebf18838a55934f`; the complete recursive editor and field-history outcomes remain open. The optional Agent console entry is not shown as a working integration. Final result remains blocked.

### Requirement conflict workflow

The reference is `/home/holyglory/kicad/automation/design/history-and-conflicts/03-save-conflict.png` (1536 × 1024 full editor scene). Compare its modal with the isolated native dialog; the test fixture deliberately has two conflicting fields and uses a field selector, whereas the reference shows one. Single-conflict dialogs identify the field beside the owner instead of displaying a disabled selector.

The first real comparison from `t20260920T003315Z-6c8204` was too tall and omitted the base/saved version labels. Its light unresolved and dark resolved captures were opened alongside the reference. The repair added model-derived revision numbers and changed the vertical sizing proportions. No revision number or actor is inferred from a timestamp or label.

The revised `t20260920T003819Z-1f2dd0` source hash is `e94d4e6fe687fa0eee54582718f5d024b0e581c0a6d6f2f84ba2da2f328cf49c`. Its 730 × 650 native window captures are materialized under `/mnt/build-storage/codex/kicad/evidence/diagram-conflict-1f2dd0/`. The source, `light/04-conflict-unresolved.png` and `dark/05-conflict-resolved.png` were opened in one comparison input. The reference's modal and these captures have similar proportions; no browser/CSS scaling is involved. Focused regions reviewed: exact version context, base/draft/saved text, no-default radio choices, resolved-text area and fixed footer.

Readable native fonts, theme foreground/backgrounds and comparison hierarchy follow the selected direction. Controls and all three text sources remain visible; fields scroll long content. Native button styling differs from the blue mock primary action, a remaining visual refinement rather than an invented action. No decorative bitmap assets are needed. The copy distinguishes comparison from saving and identifies both versions. Custom merged text and saved-text choices remain separate and do not concatenate contradictory requirements automatically.

Rendered component cases in both themes prove initial Save disabled, no default winner, all fields resolved before Save, custom text retention across field selection, cancellation and exact returned resolution identities. Full editor cases in two KiCad instances prove cancel preserving the draft and saved file, explicit resolution publishing a new revision while retaining the competing old revision, and independent fields composing without a modal. The subsequent `t20260920T003536Z-68070c` also proved another saved-file change while the modal was open rejected the old resolution, retained its text and recovered through a fresh comparison; the revised visual run repeats that scenario.

This qualifies requirement-text conflicts for block edits. It does not claim structural graph conflict resolution, connection conflict UI, complete requirement-strength interpretation, annotation workflows, or overall project readiness. Those outcomes remain open. Overall final result remains blocked on the still-missing agreed editor functionality and full qualification.

### Connection requirements and comments

The approved CPU connection inspector reference is `/home/holyglory/kicad/automation/design/level-diagram-walkthrough/03-cpu-v2-edit-controls.png`. It was opened alongside the real selected-link capture from `t20260920T005043Z-bfed53`, materialized at `/mnt/build-storage/codex/kicad/evidence/recursive-connections-bfed53/editor/4a6e7a47-e065-4823-8064-c2553bb39e14-recursive-connection.png`. The view shows the CPU local diagram and selected memory link; fixture names and connectivity are intentionally not the reference's proposed engineering solution.

The selected link is visibly highlighted and its General/Schematic/Routing fields, exact connection version and History actions are readable. P2: the initial capture omitted endpoint context and retained an irrelevant disabled Open diagram control. A subsequent repair displays real partial endpoint states and hides that block-only control. The existing hierarchy/connection navigation and draft-save journeys passed after that change in `t20260920T005659Z-a1d346`.

Rendered tests also qualified connection requirement Save/Decline and field-history restoration in `t20260920T005335Z-339af1`, preserving the later text and exact restoration source. Versioned annotation storage passed strict XML, binary messages, coordinate-free notes, original sketch points, target retention and no-op checks in `t20260920T010453Z-ea8917`. Native block and connection comments then passed Save/Decline and other-note preservation in `t20260920T011318Z-c73ac8`. None of those results establishes schematic/PCB annotation or sketch-tool readiness.

Free-space note work exposed a test-input mistake in `t20260920T012048Z-4896f0`: the helper received a click command with drag endpoint arguments, so the note was selected but never dragged. Native state and the retained screenshot agree. The corrected journey sends a real drag, and a guard rejects mismatched pointer commands before sending input. Do not describe the failed click as proof of dragging.

The corrected `t20260920T012623Z-23708c` passes, with source hash `c8f029907f09f06d3704c85535e92c0dc9f26cac45e989eee3a998dfdfa50cfd`. It proves note placement, text, actual drag, undo/redo, XML coordinates, multiple-comment selection, Save/Decline and unrelated-note preservation in two instances. The reference and `/mnt/build-storage/codex/kicad/evidence/recursive-notes-23708c/editor/d0127836-8953-4f53-bef0-a2e26b73196e-recursive-canvas-note.png` were opened together. The real 1536 × 1024 editor sits within a 1600 × 1150 capture; compare that editor region without resizing. The screenshot selects CPU while the reference selects the memory interface, so only shared layout, typography and canvas-note treatment are compared here, not inspector state equivalence. The moved note and its text are visible, and the three fields, comment selector and Save/Decline remain in the viewport. Native fonts/colors/icons are readable; no image assets are fabricated. Remaining design drift and unimplemented editor capabilities still prevent a full visual pass.

Final focused increment `t20260920T013233Z-3c11e1` also passes with source hash `aa7a68552494daddedab0da4148d54fe4c53f5c1a8accf6dab8ea43ed9505590`. It invokes the visible Note toolbar action and loads original sketch paths through typed XML while retaining all existing comments. The inspected capture is `/mnt/build-storage/codex/kicad/evidence/recursive-notes-3c11e1/editor/2697d432-f91d-4e8d-9474-ecfecfebc168-recursive-canvas-note.png`. Its test-supplied rectangular stroke appears behind its caption at the requested coordinates; that overlapping test geometry is not automatic layout qualification. Original sketch rendering/storage is not a native sketch-creation/editing tool. The final focused run repeats block/connection requirements, comments, history restoration, save conflicts, stale-resolution rejection, navigation and reopening. Overall design QA stays blocked until the remaining agreed editor scope and complete supported-state qualification are finished.

### Implementation preview, compact layout and direct controls

Existing implementation preview/Save/Decline first passed in `t20260920T014901Z-ae0513`. Its preview capture was compared with the approved CPU walkthrough in the same input. P2: the dynamically changed header label was clipped. The repair sizes the label from its current content, uses middle ellipsis for long names and keeps the full name available without wrapping the header. The preview marker is preserved.

Both themes and 1100 × 760 → 1536 × 1024 resizing were then exercised. The inspector keeps the selected owner/version above its scrolling fields, with Save/Decline fixed below. Direct mouse activation now covers Open diagram, Back/Up, Fit, Save and undo/redo, alongside keyboard input. Those tests exposed lost selection when returning from a child. Earlier focus/routing hypotheses were insufficient: the stronger starting-state trace showed System selected instead of the previously selected PSU. The native navigation function borrowed an ID from `m_path` or `m_selected` and then replaced that storage. It now owns the ID value before mutation. The exact restored-selection assertion and one-arrow acknowledgement remain in the regression; safe pointer recovery preserves a failure and continues independent downstream checks rather than hiding it.

Final scoped run `t20260920T024132Z-d32b35` passes all four development checks, including both full editor themes and both dialog themes. Its source hash is `84aa3057f8c5123615c5eb74488396067e3008cdddac4cb6d3a8f4b9a3b9ec42`; the native-UI manifest is `d01aca7df7078eb4c9a4185e03115b37edb5c7d12495d6a242f71b5d7aeac8e4`. Retained captures are materialized at `/mnt/build-storage/codex/kicad/evidence/implementation-final-d32b35/`. The normal editor is 1536 × 1024, compact is 1100 × 760, and captures include the surrounding 1600 × 1150 display. The original-resolution dark expanded capture `editor-dark/48bb1f18-b4fb-4b6b-adaa-b85a66a7627f-recursive-expanded.png` was inspected: blocks, connections, comments and stored sketch paths are present. The preview label is readable, owner context stays visible while the compact form scrolls, and action rows remain usable. Native button emphasis and broader diagram styling still differ from the mock; the overall visual result is not a final pass.

Correction: an earlier displayed image was incorrectly assessed as a blank expanded canvas. Its PNG hash (`0b7091960e29efcf83778661386e85fb186874681448ee49c5004f86eb7a6ebd`) is identical before and after the added repaint handling, and original-resolution inspection shows content. There is no reproduced blank-canvas defect to claim fixed. Full-resize repaint is preventive, and the new fixture-scoped pixel check directly verifies a populated canvas; its unit cases reject blank light/dark images and ignore minor pixel noise. Standing correction `kicad-original-pixel-evidence-before-render-defect` (`n37f77c667727f667`) requires original-pixel/hash verification before making such claims.

The final run also exercises actual compiled STDIO MCP reads of exact saved diagram levels and paged connection-field history, including wrong/incomplete target rejection. It does not qualify actual Codex Desktop or native-Mac execution. Implementation creation/removal remains open as `p5848545e243151a7`, full native schematic/PCB activation as `pe84454b4680082e1`, and complete capability discovery as `p20d75a441d50afdc`. Full structural editing, longer native history browsing, sketch creation, synchronization and the rest of the requested project remain unfinished. Final result remains blocked.

### Implementation creation, naming, removal and restoration

Management remains in the compact implementation menu and reuses KiCad's existing name-entry/confirmation patterns, as routed through Product Design. New retains requirements and boundary interfaces but starts with an empty interior. Duplicate preserves the exact saved source graph. Both keep source-revision provenance and remain unselected until Save. Removal is recoverable: archived implementations disappear from active choices but retain their design revisions, names and removal/restoration history. Active choices cannot be removed without first selecting a replacement. Notes bound to absent source children/connections are retained with explicit unresolved-target context.

The model/serialization pass `t20260920T030332Z-3443b8` and compiled management pass `t20260920T031720Z-4d9020` prove source/rename/archive history, active-removal rejection, restoration, no-op behavior, strict metadata validation and stale file guards. The initial native control pass `t20260920T032434Z-998977` exercised naming cancellation and management operations in both themes. Subsequent passes added real MCP creation/removal/restoration, all five typed agent actions, invalid-name recovery, root duplication and selecting a newly created implementation.

The approved CPU walkthrough, `/mnt/build-storage/codex/kicad/evidence/implementation-management-7c5a08/editor-light/3d83a272-48fe-410b-ba57-92ca0fdf7e94-new-implementation.png`, and the matching `...-implementation-name-error.png` were opened together at original resolution. The new-interior state intentionally has no child blocks; boundary interfaces and retained notes are visible, not a failed diagram render. The native inspector has readable requirements and explicit unresolved-note information, with Save/Decline visible. Naming error feedback identifies the conflicting name and leaves the source unchanged. Dark-theme creation was separately reviewed in `/mnt/build-storage/codex/kicad/evidence/implementation-management-b15f0e/editor-dark/ae7f6175-9b7d-4818-a859-e1e8149572c9-new-implementation.png`. These are 1536 × 1024 native editors within 1600 × 1150 display captures, not browser/CSS-scaled views. Standard native dialogs do not reproduce the mock's custom primary-button coloring; no raster assets were substituted for editable content.

Final scoped management run `t20260920T034034Z-30c9e9` passes managed contracts, native compilation and the complete focused native/STDIO journeys. It covers new/duplicate/rename/remove/restore through both native controls and the MCP client, root and child creation, cancellation, invalid-name recovery, active-choice protection, stale retries, wrong epochs, history/source preservation and saved reload. The wider editor/project remains incomplete: this does not qualify native schematic/PCB activation, full abstract-to-native synchronization, actual Codex Desktop, Mac/Windows execution, or the remaining structural/annotation tools. Overall visual QA remains blocked on that outstanding agreed scope rather than declaring the whole product complete.

### Complete requirement-history paging

The approved field-history reference remains `/home/holyglory/kicad/automation/design/history-and-conflicts/02-field-history.png`. Its modal, the final light `02-earlier-text.png`, dark `07-history-load-error.png`, and two full-editor history captures were opened together at original resolution from `/mnt/build-storage/codex/kicad/evidence/history-pages-final-4974e9/`. The normal component is 740 × 520 and compact error state is 590 × 440; full-editor captures are 1600 × 1150 around a 1536 × 1024 native editor. Compare the reference's modal region with the component, not the illustrative background. The long General-field history and stale-page state intentionally differ from the reference's short Routing example; there is no CSS or density scaling.

The first paging run `t20260920T040632Z-1ce6a5` exposed two interaction defects: later process completions were blocked while the first completion handler held the modal loop, and disabling the focused Load older button could lose Escape. The repair opens history through its own event dispatcher and moves focus to the live revision list before disabling the loading action. The complete native run `t20260920T041717Z-83fb29` then passed cancellation, older-page loading, stale-file recovery, exact restoration and independent drafts. The failed evidence is retained unchanged.

Visual review found the Saved marker could be clipped after actor text (P2). It now precedes the actor. Short complete histories do not add redundant counts or loading controls; paged histories retain a loaded/total count after their last page. Final run `t20260920T042520Z-4974e9` passes all four focused checks against source `590d40adef15464331ab21b8209b72b347d5214fa902f5b5af8433c87f68fab7`, native evidence manifest `735427fb6c038e9826b6b25cfeb65fb25615b8cb3a3b1a43896fb7d8140cc0ea`.

Focused comparison: readable native typography, clear selected-versus-saved headings, visible Saved marker, stable footer, compact wrapped error and retry text, and the original list/comparison proportions. Native light/dark controls remain distinct without relying only on color. There are no decorative assets to generate or substitute. Copy describes only available actions and genuine loading/error states. The full-editor oldest-value capture catches native scrolling in progress; it is not evidence that the final selected row is visible. The independently opened native completed-frame `dark/08-history-oldest.png` shows v1 selected and visible while saved v206 remains distinct. The full editor journey separately verifies restoring that exact identity into the draft, retaining another field and publishing a new saved revision. No visual defect is inferred from a transient scroll frame.

The rendered checks cover a 206-change history, retained selection while appending, oldest-value restoration, malformed and duplicate page rejection, a real saved-file change during inspection, cancel during loading, recovery after reload, and short-history false-positive guards. Both independent editor instances run in each theme. This closes the specific native pagination gap `p8ebf18838a55934f`, not source-instruction opening, whole-diagram history/preview/restoration, native schematic/PCB activation, platform qualification or the full editor. Overall final result remains blocked on that agreed unfinished scope.

### Whole-diagram history: first native integration pass

The source is `/home/holyglory/kicad/automation/design/history-and-conflicts/01-diagram-history.png`. The same approved right-side history panel is used at root and child levels; it does not replace the current canvas on row selection. The first native pass `t20260920T051110Z-988798` passed all four focused checks against source `86a580a1a1689cdb97888d845efb868af21fc0846d4537fcddf9122d6a1f7320`. Native evidence manifest: `505e6f27af994bbb8e848ce34915fbb9e9d1d37c952479e68a4ff52cf72038cb`.

The reference and original-resolution light inspection/dark preview captures were opened together from `/mnt/build-storage/codex/kicad/evidence/whole-history-988798/`: `editor-light/75567681-7a9e-40a5-bac6-9d9f473e2f6e-diagram-history-inspection.png` and `editor-dark/8d321443-a4a0-4d86-8c2f-d3fe6b785e48-diagram-history-preview.png`. The native editor is 1536 × 1024 inside a 1600 × 1150 display capture; the reference is a 1536 × 1024 scene. Compare the native region directly, without CSS or density scaling. The test shows the root with many revisions and a deliberately removed CPU, while the reference shows three PSU revisions; compare shared composition and inspection/preview semantics, not invented diagram contents.

Observed: row selection leaves the one-block saved diagram visible; Preview explicitly shows the former two-block diagram and its exact connections. The panel keeps Saved v230 separate from inspected/preview v229, displays actual actors and UTC times, and reports the CPU and connections removed since the inspected version. Return to current is visible during preview. Save/Decline stay disabled while history owns the inspector; restoration prepares a draft before enabling normal editing. Full native/STDIO tests prove read-only inspection and preparation, explicit preview, dirty-restore cancellation, restore then Save as a new successor and retained old roots.

Typography/spacing P2: the comparison text initially lacked the reference's heading hierarchy. The prepared follow-up uses native rich-text styling for revision and change headings, preserving the scrollable details and fixed action row. Colors follow native light/dark themes with readable selection; editable blocks remain real data-driven drawings, not bitmap assets. Names/counts/content come from the model, not mock values. Source instructions are not invented when no source provider is available; complete source opening is still missing. The compact full-editor, nested restoration, retry and dirty Save/Decline cases are added but not yet qualified. The next governed test was refused with `tests_draining` during a Coordinator upgrade; that attempt is not test or readiness evidence. Overall final result remains blocked until the follow-up is executed and the remaining agreed editor scope is complete.

### Whole-diagram history: expanded recovery and final scoped comparison

After the Coordinator upgrade, `t20260920T053424Z-0c2110` and acknowledged-input retry `t20260920T054337Z-10f1ad` exposed a real keyboard/focus failure after cancelling an in-flight history load. The new viewport/model evidence in `t20260920T055424Z-d19678` proved two resolved children and a valid viewport remained; it was not lost XML or diagram data. `t20260920T060410Z-037cc6` retained the shortcut failure and continued via the actual History button, which successfully reopened the same diagram. Loading older pages also left keyboard focus outside the revision list. These diagnostic failures remain sealed, not relabelled successes.

Repairs keep focus on live controls while reads are pending, return it only after the target is enabled, resume list inspection after an older page, and use a native accelerator for the global History shortcut. The final scoped run `t20260920T061636Z-21b360` passed against source `0f5aabbaadfeb702c92a53f67a37340fba4b49ce37d0ae55d2eff2aca85d2b4d`, native manifest `3f27e55111466ca9040844d43bf4a949f97525addfae11d04fcd2ac7827431ec`. Its journey still records any shortcut or canvas failure before safe recovery, so a passing result does not hide a workaround.

The approved whole-history reference and these original-resolution captures were opened together under `/mnt/build-storage/codex/kicad/evidence/whole-history-final-21b360/`: light `51500791-ce7e-48e6-b1bb-a42072bb1e30-diagram-history-compact.png`, dark `8490c905-6607-43c3-a0bc-dc32d38a58e7-diagram-history-child-preview.png`, and the same dark instance's `...-history-cancelled-canvas.png`. Compact editor is 1100 × 760, normal editor 1536 × 1024; both are inside 1600 × 1150 desktop captures. Background manager/schematic windows visible outside the compact editor are not clipped editor content. No CSS/density scaling was applied.

Focused review: the revision list, current-versus-inspected labels, bold comparison headings, fixed Preview/Restore controls, Return action and Save/Decline footer stay readable and visible. Both themes retain native contrast and focus cues. The historical PSU's original connected peers and boundary interfaces are displayed while Saved v2 and Preview v1 stay distinct. The post-cancellation canvas still contains the restored two-block design; exact sampled background, border and interior pixels agree with the populated preview, in addition to the compiled populated-canvas guard. There are no substitute decorative assets. Source text is fixture data, not fabricated product facts. The reference's optional source-opening link is omitted until a real provider is implemented.

Rendered checks prove root and child inspection, explicit preview/return, unchanged drafts while browsing, dirty Save/Decline/Cancel restoration, a new saved successor retaining earlier revisions and unaffected siblings, cancellation/reopening, bounded paging, invalid comparison scope, changed-file rejection/retry/reload, compact controls and both themes in independent instances. This qualifies `pb0847a7ffd99250a`; original source-instruction viewing remains open as `pb09aa30cf44e609b`. Full structural-to-native synchronization, physical realization, all editing tools and platform readiness remain unfinished. Overall final result remains blocked on that broader agreed scope, not on this now-verified history slice.

### Native multi-view MCP observations

This adds no new window or control. It exposes the existing native diagram painter through explicit, revision-bound MCP observation requests. Actual returned PNGs and structured metadata from `t20260920T062728Z-68be9d` are retained under `/mnt/build-storage/codex/kicad/evidence/diagram-observation-68be9d/editor-light/`. The `3692aa70-9875-418d-aca9-2cf291cea7f2-saved-root-view-0.png` (1024 × 768), `...-saved-root-view-1.png` (800 × 600) and `...-current-draft-view-0.png` were opened at original resolution alongside their model metadata. The root and PSU images show their distinct exact diagrams; the changed root correctly has one peer while the explicitly requested old saved root retains two. These are image contents returned by the real MCP SDK client, not desktop screenshots substituted for a missing tool.

The painter reuses native fonts, colors, labels, shapes and connection geometry; there are no new decorative assets. The metadata identifies the actual rendered viewport, logical coordinate system, source token, view revision and stable object selections. Read-only captures preserve the whole editor state. The historical-preview and post-error recovery pass `t20260920T063716Z-956bcc` adds coverage of a historical canvas while an independent draft remains retained. Final source `c6914bf323eb2ab77074f160453eaf7a40bd21ff8f0ea31385bbe63f821b64e4` passed `t20260920T064819Z-ace160`, with native manifest `442d2df3bb7c3af56c9f64246ee151d1eb023cbae37b5c96f3c858a581561ee2`. Computed zero/default values are explicit and each image is linked by content index and SHA-256. The additional formatting does not change the native painting inputs.

This qualifies the scoped native/STDIO structural observation outcome `p2349478ae4d4ef32`. It does not prove actual Codex Desktop image delivery, Mac/Windows execution, physical schematic/PCB rendering through this tool, or the full remaining editor/engineering workflow. Overall final result remains blocked on the broader open scope.

## Historical comparison — superseded option 1

The following records describe the earlier native editor and are retained as historical evidence. They are not the approved visual target for the recursive editor.

## Evidence and state

Source visual: `/home/holyglory/.codex/generated_images/01a06e57-5f4c-7b23-8476-b42467c8c6c7/exec-14f611d6-b267-4632-a0ee-71436aff425f.png` (1487 × 1058).

First native capture: `/mnt/build-storage/codex/kicad/evidence/structural-option1-528c0d/editor-evidence/6b23a438-cbca-4ab8-83f8-8c9f3fbce479-structural-initial.png` (1600 × 1150 display, 1440 × 1024 editor).

Both images were opened in the same comparison input. The source has MCU selected; the first capture has no block selected. Compare region organization and geometry only, not inspector content or exact pixel spacing. This is a native desktop surface; CSS viewport and browser device scale do not apply. A matching 1487 × 1058 editor capture with MCU selected is required for the next comparison.

## Findings

- P1: Default power paths leave through the wrong block sides and overlap block edges. The mock connects power ports from below. Draw side-aware orthogonal paths and honor stored waypoints, labels and directions.
- P2: The diagram is anchored too high and block labels are much smaller. Center fit in the available canvas and increase label hierarchy without losing truncation.
- P2: The first inspector has no selected object and omits the populated starting state of the reference. Select the initial block while keeping empty designs valid.
- P2: Custom properties can be added but cannot yet be inspected or edited. Finish their real editing controls, including quantities, before calling the editor complete.
- P2: Narrow-window and dark-theme interaction evidence is missing. Check supported desktop sizes, text scaling, keyboard focus, panel scrolling and toolbar overflow.

## Required surfaces

- Typography: native system UI font is appropriate for the KiCad platform; block label size/hierarchy needs repair. Reference mock typography is not a request to replace platform fonts.
- Spacing/layout: correct Properties-above-Hierarchy organization; centered diagram, compact properties rows and toolbar density still need a matching-state comparison.
- Colors: block fills and link colors follow the chosen direction. Dark-theme foreground/background contrast is not yet qualified.
- Images/assets: blocks, ports and lines are actual editable engineering objects, not decorative assets. Existing KiCad icons are reused; no replacement raster illustration is needed.
- Copy: native editing labels describe real operations. Source is plain text until navigation is implemented; no fake link or enabled schematic-navigation action is presented.

## Interaction evidence

Run `t20260919T155428Z-528c0d` proves real native opening in two independent instances, not the full editing journey. The save assertion in that version did not wait for an acknowledged save attempt; it cannot prove completed publication. The next fixture uses completed-save counts and changed content, then exercises undo/redo, creation cancellation, dirty-close cancellation, conflict preservation and reopen.

## Comparison history

1. First comparison found the issues above. Pending source changes address path direction, label scale, selection, fit, resizing and panning. Compilation is not post-fix visual proof.
2. Matching 1487 × 1058 capture from `t20260919T161828Z-ddaf3b`: `/mnt/build-storage/codex/kicad/evidence/structural-option1-ddaf3b/editor-evidence/1223afdb-d9f2-45fa-a0f6-a9825e2cdfe0-structural-initial.png`. Opened together with the reference. Direction arrows, side-aware paths and populated MCU inspector now render. P2: hierarchy still highlights Memory when the canvas selects MCU; synchronize both selection surfaces. P2: fit enlarges blocks more than the reference; cap automatic fit scale while preserving manual zoom. Native font and icon rendering differ by platform as expected. Fields remain stacked rather than the compact reference rows. Two real instances renamed and saved the MCU, retained circuit data and completed unchanged saves, but immediate Redo after Undo timed out; this is a functional blocker, not a visual pass. Evidence remains sealed. Later checks must qualify cancellation/conflicts/reopen, dragging and resizing.

3. Matching-state reference comparison of the passing `t20260919T163307Z-f87328` capture: `/mnt/build-storage/codex/kicad/evidence/structural-option1-f87328/editor-evidence/aa190eab-92b7-451f-9847-670f50ca1eec-structural-initial.png`. Both 1487 × 1058 images opened in one comparison input. MCU selection now agrees between canvas and hierarchy; automatic fit scale and larger block labels retain the selected composition. Native font/icon differences are expected platform rendering. P2: the inspector remains less compact than the reference; narrow-height and dark-theme checks are still missing. Saved text-property editing and optional numeric fields are new isolated source changes and have no rendered qualification yet. Keep the result blocked.

## Next checks

Capture the revised native UI at the source dimensions and state, compare both images together, then review focused inspector and canvas regions. Keep the result blocked until the actionable findings and rendered interaction gaps are resolved.
